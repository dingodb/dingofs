[pytest]
testpaths = posix quota regression specific
markers =
    smoke: quick smoke tests (< 2 min)
    standard: standard integration tests (< 10 min)
    slow: large-data, stress, or quota test excluded from merge-queue e2e
addopts = -v --tb=short
